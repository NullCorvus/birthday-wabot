{
  "name": "birthday-wabot",
  "version": "1.0.0",
  "description": "Bot automatizado de mensajes de cumpleaños por WhatsApp",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate --schema=frontend/prisma/schema.prisma",
    "db:migrate": "prisma migrate dev --schema=frontend/prisma/schema.prisma",
    "db:studio": "prisma studio --schema=frontend/prisma/schema.prisma"
  },
  "keywords": [],
  "license": "ISC",
  "devDependencies": {
    "prisma": "^7.8.0"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "dotenv": "^17.4.2",
    "pg": "^8.21.0"
  }
}
